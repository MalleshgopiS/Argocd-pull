apiVersion: apps/v1
kind: Deployment
metadata:
  name: bleater-frontend
  namespace: bleater
  labels:
    app: bleater-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bleater-frontend
  template:
    metadata:
      labels:
        app: bleater-frontend
    spec:
      containers:
        - name: frontend
          # 🔥 FIX: Use Harbor (offline Nebula registry)
          image: harbor.devops.local/library/nginx:1.25
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
          args:
            - |
              mkdir -p /usr/share/nginx/html;
              cp /wasm/app.wasm /usr/share/nginx/html/app.wasm;
              nginx -g 'daemon off;';
          volumeMounts:
            - name: wasm
              mountPath: /wasm
      volumes:
        - name: wasm
          configMap:
            name: wasm-config